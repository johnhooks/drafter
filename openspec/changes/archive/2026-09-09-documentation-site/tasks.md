## 1. Site scaffold

- [x] 1.1 Create `docs/` with Astro, Starlight, and the Nova theme, a fixed sidebar, custom CSS, and a splash index; add `pnpm-workspace.yaml` and root `docs:dev` and `docs:build` scripts; verify `pnpm docs:build` succeeds

## 2. Content

- [x] 2.1 Write Start Here: what it is, run it, your first part (sketch, extrude, pick a face, cut); verify each step against the running app
- [x] 2.2 Write Modelling: planes and sketches, extrude and bodies, timeline and face references; verify the axis and face descriptions against the specs
- [x] 2.3 Write Sketching: tools and snapping, dimensions and inline editing, viewing a back face; verify the mouse and key bindings against the app
- [x] 2.4 Write Constraints: driven slots and the keep rule, the link tool, parameters, expression reference; verify every expression example evaluates as stated using the core
- [x] 2.5 Write Files: saving and export, file format; verify the format description against a downloaded document
- [x] 2.6 Write Development: architecture and testing; verify commands run as documented

## 3. Repository conventions

- [x] 3.1 Add root `README.md` linking to the site and `AGENTS.md` with documentation conventions; verify `pnpm docs:build` passes and the site has no broken internal links

## Verification

Verified on 2026-09-13:

- `pnpm test`: 488 tests passed in 42 files.
- `pnpm test:e2e`: 48 tests passed in Chromium with software GL.
- `pnpm typecheck`: passed.
- `pnpm build`: passed with the existing large-bundle warning.
- `pnpm docs:build`: passed, 21 pages; existing optional i18n/404 and sitemap configuration warnings remain.
- `openspec validate sheet-line-hierarchy --strict` and `git diff --check`: passed.

New core tests failed before implementation, then passed with physical stroke widths and separated extensions. An additional regression exposed the exact-gap boundary at 1:12: subtraction of translated paper coordinates rounded the distance upward. The omission check now compares integer view distances against the converted paper gap; all standard ratios are covered.

Browser tests verify computed widths and extension endpoints in preview, standalone SVG, app print, and native beforeprint output at 1:4 and 1:8. They also verify reload preservation, viewport zoom independence, dimension dragging across measured points, gap omission, short-label placement, selection colours, detached warnings, and unchanged measured points and note leader endpoints.

## Visual review

Inspected the generated pocket drawing at fit-to-page and at 96 CSS pixels per paper inch. Visible outlines clearly outweigh the hidden pocket edges and annotation strokes. The short dimension, note leader, and extension gaps remain legible. Existing selection and dragging tests pass. Screenshots are generated by `tests/e2e/sheetLineHierarchy.spec.ts` at `/tmp/sheet-line-hierarchy-fit.png` and `/tmp/sheet-line-hierarchy-print.png`.

This is browser output verification, not a physical printer measurement. Isometric raster rendering, saved model geometry, annotation coordinates, text sizes, and page furniture are unchanged. A read-only core review found no remaining actionable defects.
